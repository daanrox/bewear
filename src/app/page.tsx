import { Button } from "@/components/ui/button";

const HomePage = () => {
  return <div>
    <Button className="cursor-pointer hover:bg-red-500 hover:text-white">Clique Aqui</Button>
  </div>;
};

export default HomePage;